name = "bg-remover"
main = "bg-worker.js"
compatibility_date = "2025-09-01"
compatibility_flags = ["nodejs_compat"]

# Workers AI binding — required for AI.run()
[ai]
binding = "AI"

# Optional: increase memory for large images
[limits]
cpu_ms = 30000
